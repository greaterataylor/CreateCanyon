import type {Metadata} from 'next';import Link from 'next/link';import {Brand} from '@createcanyon/web/components';import {SignOut} from '@createcanyon/web/client';import {requireSession,appUrl} from '@createcanyon/web/server';import '@createcanyon/web/styles.css';
export const dynamic='force-dynamic';export const metadata:Metadata={title:{default:'Operations · CreateCanyon',template:'%s · CreateCanyon Operations'},robots:{index:false,follow:false}};
export default async function Layout({children}:{children:React.ReactNode}){const s=await requireSession('admin');const roles=['admin','content_reviewer','payments_operator','support_agent','trust_safety'];const authorised=roles.some(r=>s.roles.includes(r));const strong=s.authenticationMethods.some(m=>['webauthn','hwk','passkey'].includes(m))&&!!s.authenticatedAt&&Math.floor(Date.now()/1000)-s.authenticatedAt<=900;return <html lang="en"><body><a className="skip-link" href="#main">Skip to content</a><header className="site-header"><div className="container header-inner"><Brand href={appUrl('storefront')}/><nav className="header-actions"><span className="badge warning">RESTRICTED OPERATIONS</span><SignOut/></nav></div></header><div className="container workspace"><aside className="sidebar"><p className="eyebrow">NETWORK OPERATIONS</p><nav aria-label="Administration">{[['','Overview'],['moderation','Content moderation'],['sellers','Seller restrictions'],['copyright','Copyright claims'],['payments','Payments'],['refunds','Refund requests'],['disputes','Payment disputes'],['support','Support'],['reviews','Review moderation'],['ledger','Financial ledger'],['jobs','Background jobs'],['audit','Audit trail']].map(([p,t])=><Link key={p} href={'/'+p}>{t}</Link>)}</nav></aside><main className="workspace-main" id="main">{!authorised?<section className="panel"><h1>Administrator access required</h1><p>Your signed-in identity does not hold an internal operations role. Customer and seller accounts cannot enter this console.</p></section>:!strong?<section className="panel"><h1>Phishing-resistant authentication required</h1><p>Use a passkey or hardware security key in the identity provider. The signed access token must assert a supported authentication method and a recent authentication time.</p><a className="button primary" href="/api/auth/login?stepUp=true&returnTo=/">Reauthenticate securely</a><p className="muted">Session methods: {s.authenticationMethods.join(', ')||'No methods asserted'}. Password-only and TOTP-only sessions cannot operate this console.</p></section>:children}</main></div></body></html>;}
