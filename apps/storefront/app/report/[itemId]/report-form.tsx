"use client";
import {useState} from 'react';import {api} from '@createcanyon/web/client';
export default function ReportForm({itemId}:{itemId:string}){const[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[done,setDone]=useState(false);return <form className="panel" style={{maxWidth:800}} onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{const r=await api('copyright-reports',{method:'POST',body:{itemId,claimantName:f.get('name'),claimantEmail:f.get('email'),allegation:f.get('allegation'),...(f.get('evidence')?{evidenceUrl:f.get('evidence')}:{}),goodFaith:f.get('goodFaith')==='on'}});setMessage(`Report ${r.id} received. ${r.message}`);setDone(true);}catch(e){setMessage(e instanceof Error?e.message:'Report failed');}finally{setBusy(false);}}}><div className="form-grid"><div className="field"><label htmlFor="name">Your name or organisation</label><input id="name" name="name" required minLength={2} maxLength={120}/></div><div className="field"><label htmlFor="email">Contact email</label><input id="email" name="email" type="email" required maxLength={320}/></div><div className="field wide"><label htmlFor="allegation">Describe the work and alleged infringement</label><textarea id="allegation" name="allegation" required minLength={30} maxLength={10000}/></div><div className="field wide"><label htmlFor="evidence">Evidence URL (optional, HTTPS)</label><input id="evidence" name="evidence" type="url" placeholder="https://" pattern="https://.*" maxLength={2000}/></div></div><div className="check-row"><input id="goodFaith" name="goodFaith" type="checkbox" required/><label htmlFor="goodFaith">I believe this report is accurate and made in good faith, and I am authorized to report the identified work.</label></div><button className="button primary" disabled={busy||done}>{busy?'Submitting…':'Submit report'}</button><p role="status" className={done?'form-status':'form-error'}>{message}</p></form>;}
