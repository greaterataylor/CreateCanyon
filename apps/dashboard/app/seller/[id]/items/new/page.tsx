import {publicApi} from '@createcanyon/web/server';import {PageHeading} from '@createcanyon/web/components';import {ItemCreate} from '../../../../../components/item-create';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const list=await publicApi<any[]>('/v1/storefronts');const channels=await Promise.all(list.map(c=>publicApi('/v1/storefronts/'+c.key)));return <><PageHeading eyebrow="UPLOAD ONCE. PUBLISH THROUGH CHANNELS." title="Create an item">Channel eligibility is loaded from the database. Irrelevant specialist storefronts cannot be selected.</PageHeading><section className="panel"><ItemCreate sellerId={id} channels={channels}/></section></>;}
