import {accountApi} from '@createcanyon/web/server';import {PageHeading,Badge} from '@createcanyon/web/components';import {ReplyForm} from '../../../components/forms';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=await accountApi('dashboard','support/'+id);return <><PageHeading title={c.subject}><Badge>{c.status}</Badge></PageHeading><section className="conversation">{c.messages.map((m:any)=><article className="panel" key={m.id}><p className="eyebrow">{m.author} · {new Date(m.created_at).toLocaleString('en-AU')}</p><p className="preserve-lines">{m.body}</p></article>)}</section>{c.status!=='CLOSED'&&<section className="panel"><ReplyForm caseId={id}/></section>}</>;}
